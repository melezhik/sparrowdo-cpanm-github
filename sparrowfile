#!perl6

# Example

module-run 'Cpanm::GitHub', %(
    user    => "melezhik",      # github user, required
    project => "swat",          # GitHub project
    branch  => "master"         # Branch name; If not set master branch installed
);
